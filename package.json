{
  "name": "recipes-osborne",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "build": "node scripts/build.mjs",
    "preview": "npm run build && npx wrangler dev --local",
    "deploy": "npm run build && npx wrangler deploy"
  },
  "devDependencies": {
    "marked": "^17.0.1",
    "wrangler": "^4.72.0"
  }
}
